1- Compras ->

Necesidades :  {Se añade texto al trabajo ya existente}

F1 : {
	Pedido de reaprovisionamiento : Se elimina la (A) de algunos campos que realmente no se darán el alta en ninguna tabla sino que solo se usan de consulta.

	control de proveedores : {

		tabla proveedores : { 
		-Eliminación de la (A) de varios campos que no serán ingresados en tablas
		-Eliminación de la funcionalidad fecha de modificacion junto con su campo asociado
		}

		tabla cotizaciones : { 
		-Eliminación de el campo fecha de emision que no era usado en ningún otro lado mas que en este documento
		-Eliminación de todas las (A) que no coinciden con una inserción real en ninguna tabla.
		-Eliminación de todas las modificaciones
		-Eliminación de la restriccion del campo "vigente" al intentar borrar una cotizacion ya que nunca se implementó en la base de datos.

		-tabla bancos : AHORA LOS PROVEEDORES SON LOS QUE DAN EL ALTA A LOS BANCOS, se añade esa funcionalidad.
		
		}
	}

	 Remito : Eliminación de las letras (A) de los campos que no se utilizarán realmente en un alta.

	Cuentas corrientes proveeodores : {
		-La entidad se modifica sustancialmente en funcionalidad
		-No le dará el alta a ciertos campos antes descritos como Banco y numero de cuenta.
		-Se cambia la restriccion de eliminacion, en vez de poder eliminarse solo si se encuentran "debe" o "haber" en nulo, ahora solo podrán eliminarse
		si se encuentra en 0.
	}

	Registracion : Eliminacion de la (A) de los campos que no se ingresarán realmente en una tabla.
}

Modelo de datos : {
		-Eliminación de la relacion que va desde la tabla bienes de uso a pedido de reaprovisionamiento
		-Se añade el campo fecha de creacion al informe de recepcion			
 }.

Base de datos : {

	En la tabla 1_informe_recepcion : Se añade el campo fecha_creacion 
}

Diccionario de datos : {
	
	En la tabla 1_informe_recepcion : Se añade el campo fecha_creacion 

}